pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
                echo "✅ Repo successfully checked out!"
            }
        }
    }
}
